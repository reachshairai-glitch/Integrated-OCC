import React from "react";
import { createRoot } from "react-dom/client";
import IOCCPrototype from "./IOCCPrototype.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <IOCCPrototype />
  </React.StrictMode>
);
